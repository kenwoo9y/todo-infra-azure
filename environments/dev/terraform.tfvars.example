# Azure Subscription ID (Required)
# subscription_id = "your-subscription-id"

# Azure Region (Optional)
location     = "japaneast"

# Database passwords (Required)
mysql_password = "your-mysql-secure-password"
postgresql_password = "your-postgresql-secure-password"

# Environment settings (Optional)
environment = "dev"
name_prefix = "todo"

# Container Image (Optional)
# Leave empty for initial deployment to create infrastructure first (Container Registry, databases, etc.)
# After pushing your Docker image to Container Registry, set this value and redeploy to create Container Apps service
# Example: "<your-registry-name>.azurecr.io/todo-backend:latest"

# Default database type (Optional)
# Choose "mysql" or "postgresql"
default_database_type = "mysql"

# Database names and user names (Optional)
mysql_database_name = "todo_mysql_db"
postgresql_database_name = "todo_postgresql_db"
mysql_user = "todo_mysql_user"
postgresql_user = "todo_postgresql_user"

# Log Analytics Workspace settings (Optional)
log_analytics_workspace_sku = "PerGB2018"

# Container App resource settings (Optional)
container_app_cpu = 0.25
container_app_memory = "0.5Gi"
container_app_target_port = 8000

# Terraform Service Principal Object ID (Optional)
# This should be set to the same service principal object_id used in CI/CD (GitHub Actions)
# to ensure consistent Key Vault access policy across all execution environments.
# If not set, will use the current authenticated user/service principal's object_id.
# Get this value using: az ad sp show --id <CLIENT_ID> --query id -o tsv
# terraform_service_principal_object_id = "your-service-principal-object-id"